# Licensed under the GNU General Public License v. 3 (the "License")
# You may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     https://www.gnu.org/licenses/gpl-3.0.html
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# Copyright (C) 2022-2024 xqyjlj<xqyjlj@126.com>
#
# @author      xqyjlj
# @file        build.yml
#
# Change Logs:
# Date           Author       Notes
# ------------   ----------   -----------------------------------------------
# 2024-02-21     xqyjlj       initial version
#

name: 👷 build

on:
  push:
  pull_request:

jobs:
  build_examples:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target:
          - examples/led
    env:
      XMAKE_ROOT: "y"

    steps:
      - name: ⬇️ checkout csp_hal_apm32f1
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: ⬇️ install xmake
        uses: xmake-io/github-action-setup-xmake@v1

      - name: ⬇️ install cmake
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake

      - name: ⬇️ install toolchains
        shell: bash
        run: |
          mkdir -pv /opt
          pushd /opt
          wget https://github.com/csplink/toolchains/releases/download/arm-none-eabi/gcc-arm-none-eabi-10.3-2021.10-x86_64-linux.tar.bz2
          tar -xjf gcc-arm-none-eabi-10.3-2021.10-x86_64-linux.tar.bz2
          export PATH="/opt/gcc-arm-none-eabi-10.3-2021.10/arm-none-eabi/bin/:${PATH}"
          popd

      - name: 👷 build xmake
        shell: bash
        run: |
          pushd ${{ matrix.target }}
            xmake f -m release --buildir=build.xmake -P . -y
            xmake -j$(nproc) -v -P . -y
            xmake f -m debug --buildir=build.xmake -P . -y
            xmake -j$(nproc) -v -P . -y
          popd

      - name: 👷 build cmake
        shell: bash
        run: |
          pushd ${{ matrix.target }}
            mkdir -pv build.cmake
            pushd build.cmake
              cmake .. -DCMAKE_BUILD_TYPE=Release
              make -j$(nproc) VERBOSE=1
              cmake .. -DCMAKE_BUILD_TYPE=Debug
              make -j$(nproc) VERBOSE=1
            popd
          popd
